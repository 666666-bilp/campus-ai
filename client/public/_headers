# Cloudflare Pages 自定义 HTTP 头
# 所有 HTML 文件不缓存（确保用户获取最新版本）
/*.html
  Cache-Control: no-cache

# 静态资源长期缓存（带哈希的文件名）
/assets/*
  Cache-Control: public, max-age=31536000, immutable

# API 代理（开发环境使用，生产环境由 Render 处理）
# /api/*
#   Access-Control-Allow-Origin: *
