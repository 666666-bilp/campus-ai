# Cloudflare Pages 配置文件
# 使用 Wrangler CLI 部署: npx wrangler pages deploy dist

name = "academic-ai-platform"
compatibility_date = "2025-01-01"

# Pages 项目配置
[env.production]
# 生产环境的环境变量
[vars]
# 注意: 前端无法直接访问后端环境变量
# VITE_API_URL 在 .env.production 中配置

# 如果需要使用 Cloudflare Pages Functions（API 代理等）
# 可以取消下面的注释并在 /functions 目录中编写函数
# [build]
# command = "npm run build"
# output = "dist"

# ==========================================
# 后端 Workers 配置（可选：如需将后端迁移到 Workers）
# 在 server 目录另行创建 wrangler.toml
# ==========================================
